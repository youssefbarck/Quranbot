# سجل التغييرات — بوت الحصون الخمسة

## الإصدار 4.1.1 (إصلاحات حرجة)

### إصلاحات الأعطال (Bugs Fixed)

1. **إضافة معالجة أخطاء شاملة لـ `/start`**
   - `start_command` كان ينهار بدون رسالة واضحة عند أي خطأ في قاعدة البيانات
   - الآن يلتقط الاستثناء ويعرض رسالة تنبيه بدلاً من الانهيار الكامل

2. **إضافة معالجة أخطاء لـ `show_main_panel` و `show_today_dashboard`**
   - كانت أي مشكلة في قاعدة البيانات تسبب رسالة "خطأ غير متوقع" غامضة
   - الآن يعرض رسالة واضحة مع اقتراح إعادة `/start`

3. **إصلاح `hizb_to_pages` يستخدم ثابتًا خاطئًا**
   - كان يستخدم `config.DAILY_LISTENING_PAGES` (الذي يحدث أن قيمته 10)
   - أُضيف ثابت `PAGES_PER_HIZB = 10` واستُبدل الاستخدام الخاطئ

4. **إصلاح Onboarding: الإدخال النصي المخصص غير معالَج**
   - خطوة 2 (مقدار يومي مخصص): كان النقر على "مخصص" يعيّن الحالة لكن لا يوجد معالج
   - خطوة 3 (مقدار أسبوعي مخصص): لم يكن موجودًا أصلاً
   - خطوة 4 (تاريخ مخصص): لم يكن موجودًا أصلاً
   - أُضيف دالة `_handle_onboarding_text` تعالج كل الخطوات

5. **إصلاح: NLP "حفظت الوجه X" كان يحذف كل سجل المحفوظ**
   - كان يستدعي `set_initial_hifz` الذي يحذف كل `MemorizationLog` السابقة
   - الآن يحدّث `last_hifz_page` فقط ويُضيف الأوجه الجديدة للسجل

6. **إصلاح: NLP "صفحة X" / "سورة Y" كان يستدعي `set_initial_hifz` المُدمِّر**
   - استُبدل بـ `set_last_hifz_page` الذي يُحدّث بدون حذف السجل

### تحسينات (Improvements)

7. **حماية من رسائل طويلة جدًا**
   - `safe_edit_message` و `safe_send_message` تقطع النص عند 4000 حرف
   - تجنّبًا لخطأ `BadRequest` من تيليجرام (حد 4096 حرف)

8. **تحسين logging في معالج الأخطاء**
   - يُسجّل الآن `user_id` و `chat_id` مع كل خطأ غير متوقع
   - يُسهّل التشخيص بشكل كبير

9. **تنظيف التعريفات المكررة لـ `logger`**
   - أُزيلت 12 تعريفًا مكررًا لـ `logger = logging.getLogger(__name__)`
   - بقي تعريف واحد في بداية الملف وواحد في النهاية

10. **دعم الكمية الأسبوعية المخصصة في Onboarding**
    - أُضيف خيار "مخصص" في خطوة الأسبوعي مع معالجة الإدخال النصي
